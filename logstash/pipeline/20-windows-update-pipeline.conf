input {
  pipeline { address => windows_update_pipeline }
}
filter {
  mutate {
    add_field => { "[@metadata][target_index]" => "windows-update-logs" }
  }
}
output {
  pipeline { send_to => [es_output_pipeline] }
}